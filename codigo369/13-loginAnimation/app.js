// const figure = document.querySelectorAll('.card__figure');

// const getRandom = (min, max) => {
//     return Math.random() * (max - min) + min;
//   };

// for (let i = 0; i < 5; i++) {
//   figure.style.animationDuration = `${getRandom(2, 7)}s`;
// }